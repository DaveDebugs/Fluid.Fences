<Window x:Class="DesktopFences.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fence" Height="300" Width="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        AllowDrop="True" 
        Drop="Window_Drop"
        Loaded="Window_Loaded"
        ShowInTaskbar="False">
    <Border Background="#B2000000" CornerRadius="8" BorderBrush="#44FFFFFF" BorderThickness="1" Margin="10">

        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.5" Direction="270"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#22FFFFFF" CornerRadius="7,7,0,0" 
                    MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <TextBlock Text="My Apps" Foreground="White" VerticalAlignment="Center" 
                           HorizontalAlignment="Center" FontFamily="Segoe UI Variable, Segoe UI" 
                           FontWeight="SemiBold" FontSize="14"/>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="IconPanel" Margin="10" Orientation="Horizontal">
                    <TextBlock Text="Drop shortcuts here..." Foreground="#88FFFFFF" 
                               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                </WrapPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>