<Window x:Class="DesktopFences.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fluid Fences Settings" Height="600" Width="850" 
        WindowStartupLocation="CenterScreen" Background="#1E1E1E" Foreground="White"
        FontFamily="Segoe UI Variable, Segoe UI, Arial">

    <Window.Resources>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#151515" BorderBrush="#333" BorderThickness="0,0,1,0"/>
                            <StackPanel Grid.Column="0" IsItemsHost="True" Margin="0,20,0,0"/>
                            <ContentPresenter Grid.Column="1" x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Margin="20"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="5,2">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#0078D7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF4444"/>
            <Setter Property="BorderBrush" Value="#FF4444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33FF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#0078D7"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ListBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ListBorder" Property="Background" Value="#0078D7"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ListBorder" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl SelectedIndex="0">

            <TabItem Header="About">
                <Grid Margin="40">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Width="80" Height="80" Background="#0078D7" CornerRadius="15" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <TextBlock Text="FF" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Fluid Fences" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <TextBlock Text="Fluid Fences is a streamlined solution for organizing your desktop. Create, resize, and manage your fence regions effortlessly. Centralize your shortcuts and files to clear the clutter and reclaim your workspace." 
                                   TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="#CCCCCC" LineHeight="22" MaxWidth="500"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Fences">
                <Grid Margin="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,30,0">
                        <TextBlock Text="Select a Fence" Style="{StaticResource SectionHeader}"/>
                        <ListBox x:Name="FenceListBox" Background="#222" Foreground="White" BorderBrush="#444" Height="400" SelectionChanged="FenceListBox_SelectionChanged">
                        </ListBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="20,0,0,0">
                        <TextBlock Text="Fence Settings" Style="{StaticResource SectionHeader}"/>

                        <TextBlock Text="Fence Title" Foreground="#AAA" Margin="0,0,0,5"/>
                        <TextBox x:Name="TitleInput" Background="#222" Foreground="White" BorderBrush="#444" Padding="8" Margin="0,0,0,20"/>

                        <TextBlock Text="Sort by" Foreground="#AAA" Margin="0,0,0,5"/>
                        <ComboBox x:Name="SortDropdown" Background="#222" Foreground="White" BorderBrush="#444" Padding="8" Margin="0,0,0,20">
                            <ComboBoxItem Content="None"/>
                            <ComboBoxItem Content="Name"/>
                            <ComboBoxItem Content="Size"/>
                            <ComboBoxItem Content="Item Type"/>
                            <ComboBoxItem Content="Date Modified"/>
                            <ComboBoxItem Content="Date Created"/>
                            <ComboBoxItem Content="Date added to Fence"/>
                        </ComboBox>

                        <TextBlock Text="View" Style="{StaticResource SectionHeader}" FontSize="16" Margin="0,10,0,15"/>
                        <CheckBox x:Name="HideEmptyCheckbox" Content="Hide when empty" Foreground="White" Margin="0,0,0,30"/>

                        <Border BorderBrush="#444" BorderThickness="0,1,0,0" Margin="0,20,0,20"/>

                        <Button x:Name="DeleteFenceBtn" Content="Delete This Fence" Style="{StaticResource DangerButton}" Padding="15,8" HorizontalAlignment="Left" Click="DeleteFenceBtn_Click"/>

                        <Button Click="SaveSettings_Click" Content="Save Changes" Style="{StaticResource PrimaryButton}" Padding="20,10" HorizontalAlignment="Right" Margin="0,40,0,0"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Desktop">
                <Grid Margin="40">
                    <StackPanel>
                        <TextBlock Text="Desktop Settings" Style="{StaticResource SectionHeader}"/>
                        <CheckBox x:Name="AlignRowCheckbox" Content="Always align fences in one row" Foreground="White" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Feedback">
                <Grid Margin="40">
                    <StackPanel>
                        <TextBlock Text="We'd love to hear from you" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Have a question or a brilliant idea to make Fluid Fences even better? Let us know!" Foreground="#AAA" Margin="0,0,0,20"/>

                        <Button Content="Ask a question" Padding="15,10" HorizontalAlignment="Left" Margin="0,0,0,10" Width="180"/>
                        <Button Content="I have a suggestion" Padding="15,10" HorizontalAlignment="Left" Width="180"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Help">
                <Grid Margin="40">
                    <StackPanel>
                        <TextBlock Text="Help &amp; Support" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Learn how to get the most out of Fluid Fences or fix common issues." Foreground="#AAA" Margin="0,0,0,20"/>

                        <Button Content="Introduction" Padding="15,10" HorizontalAlignment="Left" Margin="0,0,0,10" Width="180"/>
                        <Button Content="Troubleshooting" Padding="15,10" HorizontalAlignment="Left" Width="180"/>
                    </StackPanel>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>